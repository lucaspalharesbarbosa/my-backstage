# Variáveis carregadas pelo docker-compose.elasticsearch.yml (apenas dev local).
# Em máquina compartilhada, altere ELASTIC_PASSWORD antes do primeiro `docker compose up`.
ELASTIC_PASSWORD=elasticsearch_my_backstage
ES_USER=elastic