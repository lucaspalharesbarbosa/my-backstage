# https://backstage.io/docs/features/software-catalog/descriptor-format#kind-component
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: backstage-deploy-pipeline
  title: Backstage deploy pipeline
  description: >-
    Pipeline de implantação do plugin nas instâncias Backstage (Digital/TI),
    com trilha de GMUD.
spec:
  type: library
  lifecycle: production
  owner: group:default/squad-gerador-scaffolding
  system: system:default/gerador-scaffolding
